# Production profile overrides
# Data-initialization runners are disabled by default in production.
# Set to true (via environment variable or explicit config) only when a
# one-time data migration/seed is required, then revert before the next deploy.
app.runners.backfill-checkin=false
app.runners.seed-lich-trinh=false
app.runners.init-data=false
